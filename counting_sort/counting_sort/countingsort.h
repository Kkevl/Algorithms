#pragma once
#include<iostream>
#include<vector>
#include<chrono>
#include<ctime>
using namespace std;
class countingsort
{
public:
	void sorter(vector<int> &array1, vector<int>& array2,int b);
	void printarray(vector<int> &array);
};

